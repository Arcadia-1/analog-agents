# Project-level effort configuration for analog-agents.
# Command-line --effort overrides this file.
# Default: standard

default_effort: standard

# Per block-type overrides (optional):
# overrides:
#   comparator: intensive
#   bias: lite

# EDA mode (optional):
# full    — requires servers.yml, runs Spectre simulations
# review  — no EDA needed, uses checklists + cross-model review + hand-calc validation
# auto    — detect from servers.yml presence (default)
# eda_mode: auto
